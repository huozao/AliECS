(async()=>{const status=document.querySelector('#status'), root=document.querySelector('#contracts'); const esc=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); async function load(){const r=await fetch('/api/v1/market/realtime',{cache:'no-store'}); if(!r.ok) throw new Error(`HTTP ${r.status}`); const d=await r.json(); const rows=new Map((d.quotes||[]).map(x=>[x.contract,x])); root.innerHTML=[...rows.entries()].sort().map(([c,q])=>`<article class="card"><h2>${esc(c)}</h2><div class="value">${q.last_price==null?'—':Number(q.last_price).toFixed(2)}</div><div class="muted">最近 ${d.window_minutes} 分钟 · 挂单 ${esc(q.order_status||'—')}</div></article>`).join(''); status.textContent=`实时窗口：${d.window_minutes} 分钟；合约 ${rows.size} 个；更新时间 ${esc(d.updated_at||'—')}`;} try{await load(); setInterval(load,2000);}catch(e){status.textContent=`实时数据不可用：${e.message}`;}})();
