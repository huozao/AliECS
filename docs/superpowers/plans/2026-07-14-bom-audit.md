# bom-builder BOM 审核功能 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 bom-builder 同页加「录入 / 审核」Tab，审核 Tab 实时拉 T+ 未审 BOM（本工具建的 / 全部可切）、行内看子件明细、逐条真调 T+ 审核并复查、审核后同步消失。

**Architecture:** 后端两个同步端点 `GET /v1/tplus/bom-pending` 与 `POST /v1/tplus/bom-audit`（不走 write-worker 队列，审核是单次快操作）；纯领域函数（未审判定/子件抽取/审核 payload/提交键抽取）抽到 `app/tplus_bom.py` 可无 HTTP 单测；前端单文件加 Tab 与审核列表。防假装靠"列表实时查 T+ + 审核后复查 VoucherState"。

**Tech Stack:** FastAPI + pydantic + psycopg；原生 JS 单页；pytest + unittest。

## Global Constraints

- spec：`docs/superpowers/specs/2026-07-14-bom-audit-design.md`
- 权威判据：T+ `bom/Query` 返回的 `VoucherState`，未审=`Code="00"`（Name="未审"）。
- 防假装三保证：①列表实时查 T+（非查本地 tplus_bom_submissions）②审核真调 `bom/Audit` 后**立即** `bom/Query` 复查翻已审才算成功/移除 ③刷新重拉实时状态。
- 审核为同步端点，不入 write-worker 队列。
- 新权限 `tplus.bom.audit`，与 `tplus.bom.write` 分离，迁移默认授 admin 角色。
- ⚠️ **T+ `bom/Audit` 精确参数形态未敲定**（探测时 T+ 限流 EXSV0011）：端点用 `build_audit_payload()` 单函数封装，Task 7 部署后用测试 BOM 现场探定，只调这一个函数。`bom/Query` 列表分页形态（`param`）同样待现场确认。
- backend 测试：仓库根 `python -m pytest tests/ -q`（Windows PowerShell 同）。
- 不新增依赖。DB 迁移编号接 0027 → **0028**。
- git 提交只显式 add 本任务列出的文件，禁 `-A`/`.`/`-u`。
- 分支 `feat/bom-audit`（已基于 origin/main 建好，spec 已提交）。

---

### Task 1: 迁移——新增 tplus.bom.audit 权限并授予 admin

**Files:**
- Create: `db/migrations/0028_tplus_bom_audit.sql`
- Test: `tests/test_tplus_bom_builder_frontend.py`（迁移断言，复用现有 migration 读取模式）

**Interfaces:**
- Produces: 权限码 `tplus.bom.audit`。后端 Task 3/4 用 `require_permission("tplus.bom.audit", user)` 校验。

- [ ] **Step 1: 写失败测试**

`tests/test_tplus_bom_builder_frontend.py` 的 `setUpClass` 已读 `0026_tplus_bom_write.sql`；新增独立读取断言（类末尾、`if __name__` 前）：

```python
    def test_audit_migration_adds_permission(self):
        from pathlib import Path
        sql = (ROOT / "db" / "migrations" / "0028_tplus_bom_audit.sql").read_text(encoding="utf-8")
        self.assertIn("tplus.bom.audit", sql)
        self.assertIn("role_permissions", sql)
        self.assertIn("admin", sql)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_tplus_bom_builder_frontend.py::TPlusBomBuilderFrontendTests::test_audit_migration_adds_permission -q`
Expected: FAIL（文件不存在 FileNotFoundError）

- [ ] **Step 3: 写迁移**

`db/migrations/0028_tplus_bom_audit.sql`：

```sql
-- T+ BOM 审核权限（与写入分离，为将来提交人≠审核人留口）
INSERT INTO permissions(code, name, description) VALUES
('tplus.bom.audit', 'T+ BOM 审核', '审核 T+ 物料清单（调用 T+ bom/Audit）')
ON CONFLICT (code) DO NOTHING;

WITH admin_role AS (SELECT id FROM roles WHERE code = 'admin')
INSERT INTO role_permissions(role_id, permission_id)
SELECT admin_role.id, p.id
FROM admin_role, permissions p
WHERE p.code = 'tplus.bom.audit'
ON CONFLICT DO NOTHING;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_tplus_bom_builder_frontend.py -q`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add db/migrations/0028_tplus_bom_audit.sql tests/test_tplus_bom_builder_frontend.py
git commit -m "feat(tplus): 新增 tplus.bom.audit 权限迁移"
```

---

### Task 2: 领域纯函数（未审判定 / 子件抽取 / pending item / 审核 payload / 提交键）

**Files:**
- Modify: `services/backend-api/app/tplus_bom.py`（文件末尾追加）
- Test: `tests/test_tplus_bom_write.py`（复用现有 importlib 加载 `tplus_bom.py` 的模式）

**Interfaces:**
- Produces（供 Task 3/4 端点消费）：
  - `voucher_is_pending(bom: dict) -> bool` — `VoucherState.Code == "00"`
  - `bom_children(bom: dict) -> list[dict]` — `[{"code","name","unit_name","required_quantity"}]`，容忍 T+ 多种键名
  - `pending_item(bom: dict) -> dict` — `{"code","name","version","produce_quantity","voucher_state":{"code","name"},"bom_id","children":[...]}`
  - `submission_bom_key(request_json: dict) -> tuple[str, str] | None` — 从提交记录抽 (parent_code, version)
  - `build_audit_payload(code: str, version: str, bom_id: str) -> dict` — T+ 审核请求体（超集 dto，Task 7 现场校准）

- [ ] **Step 1: 写失败测试**

`tests/test_tplus_bom_write.py` 类内追加：

```python
    def test_voucher_is_pending_only_for_code_00(self):
        self.assertTrue(main.voucher_is_pending({"VoucherState": {"Code": "00", "Name": "未审"}}))
        self.assertFalse(main.voucher_is_pending({"VoucherState": {"Code": "01", "Name": "已审"}}))
        self.assertFalse(main.voucher_is_pending({}))

    def test_bom_children_tolerates_key_variants(self):
        bom = {"BOMChildDTOs": [
            {"Inventory": {"Code": "RM-1", "Name": "原料一"}, "Unit": {"Name": "kg"}, "RequiredQuantity": "2.5"},
            {"InventoryCode": "RM-2", "InventoryName": "原料二", "BaseUnitName": "个", "RequireQty": "1"},
        ]}
        children = main.bom_children(bom)
        self.assertEqual(["RM-1", "RM-2"], [c["code"] for c in children])
        self.assertEqual(["原料一", "原料二"], [c["name"] for c in children])
        self.assertEqual(["kg", "个"], [c["unit_name"] for c in children])
        self.assertEqual(["2.5", "1"], [c["required_quantity"] for c in children])

    def test_pending_item_shape(self):
        bom = {
            "Code": "06000001", "Version": "260713F", "ID": 267,
            "Inventory": {"Name": "hyd测试"}, "ProduceQuantity": "5",
            "VoucherState": {"Code": "00", "Name": "未审"}, "BOMChildDTOs": [],
        }
        item = main.pending_item(bom)
        self.assertEqual("06000001", item["code"])
        self.assertEqual("hyd测试", item["name"])
        self.assertEqual("260713F", item["version"])
        self.assertEqual("267", item["bom_id"])
        self.assertEqual("00", item["voucher_state"]["code"])
        self.assertEqual([], item["children"])

    def test_submission_bom_key_from_envelope(self):
        req = {"bom": {"dto": {"Inventory": {"Code": "06000001"}, "Version": "260713F"}}}
        self.assertEqual(("06000001", "260713F"), main.submission_bom_key(req))
        self.assertIsNone(main.submission_bom_key({"bom": {"dto": {}}}))

    def test_build_audit_payload_carries_business_keys(self):
        payload = main.build_audit_payload("06000001", "260713F", "267")
        dto = payload["dto"]
        self.assertEqual("06000001", dto["Code"])
        self.assertEqual("260713F", dto["Version"])
        self.assertEqual("267", str(dto["ID"]))
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_tplus_bom_write.py -q`
Expected: 5 个新测试 FAIL（AttributeError 无此函数）

- [ ] **Step 3: 实现（`services/backend-api/app/tplus_bom.py` 末尾追加）**

```python
def _first(bom: dict[str, Any], *keys: str) -> Any:
    for key in keys:
        if "." in key:
            head, tail = key.split(".", 1)
            nested = bom.get(head)
            if isinstance(nested, dict) and tail in nested and _text(nested[tail]):
                return nested[tail]
        elif key in bom and _text(bom[key]):
            return bom[key]
    return ""


def voucher_is_pending(bom: dict[str, Any]) -> bool:
    state = bom.get("VoucherState") or {}
    return _text(state.get("Code")) == "00"


def bom_children(bom: dict[str, Any]) -> list[dict[str, Any]]:
    rows = bom.get("BOMChildDTOs") or bom.get("Children") or []
    children: list[dict[str, Any]] = []
    for row in rows:
        if not isinstance(row, dict):
            continue
        children.append({
            "code": _text(_first(row, "Inventory.Code", "InventoryCode", "Code")),
            "name": _text(_first(row, "Inventory.Name", "InventoryName", "Name")),
            "unit_name": _text(_first(row, "Unit.Name", "BaseUnitName", "UnitName")),
            "required_quantity": _text(_first(row, "RequiredQuantity", "RequireQty", "Quantity")),
        })
    return children


def pending_item(bom: dict[str, Any]) -> dict[str, Any]:
    state = bom.get("VoucherState") or {}
    return {
        "code": _text(_first(bom, "Code", "Inventory.Code")),
        "name": _text(_first(bom, "Inventory.Name", "Name")),
        "version": _text(bom.get("Version")),
        "produce_quantity": _text(bom.get("ProduceQuantity")),
        "voucher_state": {"code": _text(state.get("Code")), "name": _text(state.get("Name"))},
        "bom_id": _text(_first(bom, "ID", "Id", "id")),
        "children": bom_children(bom),
    }


def submission_bom_key(request_json: dict[str, Any]) -> tuple[str, str] | None:
    payload = request_json.get("bom") or request_json
    dto = (payload or {}).get("dto") or {}
    code = _text((dto.get("Inventory") or {}).get("Code"))
    version = _text(dto.get("Version"))
    if code and version:
        return code, version
    return None


def build_audit_payload(code: str, version: str, bom_id: str) -> dict[str, Any]:
    # T+ bom/Audit：带业务键超集，ID 优先、Code+Version 兜底（确切形态由 Task 7 现场校准）。
    dto: dict[str, Any] = {"Code": _text(code), "Version": _text(version)}
    if _text(bom_id):
        dto["ID"] = _text(bom_id)
    return {"dto": dto}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_tplus_bom_write.py -q`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add services/backend-api/app/tplus_bom.py tests/test_tplus_bom_write.py
git commit -m "feat(tplus): BOM 审核领域函数（未审判定/子件抽取/pending item/审核payload）"
```

---

### Task 3: `GET /v1/tplus/bom-pending` 端点

**Files:**
- Modify: `services/backend-api/app/routers/tplus_bom.py`
- Test: `tests/test_backend_tplus_bom_picker.py`

**Interfaces:**
- Consumes: Task 2 的 `pending_item`/`voucher_is_pending`/`submission_bom_key`。
- Produces: `GET /v1/tplus/bom-pending?scope=mine|all` → `{"items":[pending_item…], "scope", "synced_at"}`；供前端 Task 5 消费。模块级新增 `_load_success_submissions()`（读 DB，测试打桩）与 `_chanjet_business_post()`（带原文透传，Task 4 复用）。

- [ ] **Step 1: 写失败测试**

`tests/test_backend_tplus_bom_picker.py` 末尾（`if __name__` 前）追加：

```python
class TPlusBomPendingTests(unittest.TestCase):
    @classmethod
    def setUpClass(cls) -> None:
        for name in list(sys.modules):
            if name == "app" or name.startswith("app."):
                del sys.modules[name]
        sys.path.insert(0, str(BACKEND_ROOT))
        from app.routers import tplus_bom as module
        cls.main = module

    @classmethod
    def tearDownClass(cls) -> None:
        for name in list(sys.modules):
            if name == "app" or name.startswith("app."):
                del sys.modules[name]
        sys.path[:] = [item for item in sys.path if item != str(BACKEND_ROOT)]

    def _user(self) -> dict:
        return {"sub": "tester", "roles": [], "permissions": ["tplus.bom.audit"]}

    def test_mine_scope_returns_only_pending(self):
        self.main._load_success_submissions = lambda: [
            {"bom": {"dto": {"Inventory": {"Code": "06000001"}, "Version": "V1"}}},
            {"bom": {"dto": {"Inventory": {"Code": "06000002"}, "Version": "V1"}}},
        ]
        def fake_query(endpoint, payload):
            code = payload["dto"]["Code"]
            state = "00" if code == "06000001" else "01"
            return [{"Code": code, "Version": "V1", "ID": 1, "VoucherState": {"Code": state, "Name": "x"}, "BOMChildDTOs": []}]
        self.main._chanjet_read_post = fake_query
        result = self.main.tplus_bom_pending(scope="mine", user=self._user())
        self.assertEqual(["06000001"], [i["code"] for i in result["items"]])

    def test_all_scope_filters_pending_from_list(self):
        self.main._chanjet_read_post = lambda endpoint, payload: [
            {"Code": "A", "Version": "V1", "ID": 1, "VoucherState": {"Code": "00", "Name": "未审"}, "BOMChildDTOs": []},
            {"Code": "B", "Version": "V1", "ID": 2, "VoucherState": {"Code": "01", "Name": "已审"}, "BOMChildDTOs": []},
        ]
        result = self.main.tplus_bom_pending(scope="all", user=self._user())
        self.assertEqual(["A"], [i["code"] for i in result["items"]])

    def test_requires_audit_permission(self):
        from fastapi import HTTPException
        with self.assertRaises(HTTPException) as ctx:
            self.main.tplus_bom_pending(scope="mine", user={"sub": "x", "roles": [], "permissions": []})
        self.assertEqual(403, ctx.exception.status_code)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_backend_tplus_bom_picker.py::TPlusBomPendingTests -q`
Expected: FAIL（无 `tplus_bom_pending` / `_load_success_submissions`）

- [ ] **Step 3: 实现**

`services/backend-api/app/routers/tplus_bom.py` import 区补齐（若缺）：文件顶部 `from app.tplus_bom import ...` 追加 `pending_item, voucher_is_pending, submission_bom_key, build_audit_payload`。

`app.core` 导入行确认含 `require_permission`（已有）。模块级加：

```python
BOM_QUERY_ENDPOINT = "/tplus/api/v2/bom/Query"
BOM_AUDIT_ENDPOINT = "/tplus/api/v2/bom/Audit"


def _require_bom_audit(user: dict[str, Any]) -> None:
    require_permission("tplus.bom.audit", user)


def _load_success_submissions() -> list[dict[str, Any]]:
    with closing(_conn()) as conn:
        with conn.cursor() as cur:
            cur.execute(
                "SELECT request_json FROM tplus_bom_submissions WHERE status='success' ORDER BY id DESC LIMIT 500"
            )
            return [row[0] for row in cur.fetchall()]


def _bom_query_rows(response: Any) -> list[dict[str, Any]]:
    if isinstance(response, list):
        return [r for r in response if isinstance(r, dict)]
    if isinstance(response, dict):
        for key in ("result", "Result", "data", "Data", "value", "Value", "rows", "Rows"):
            value = response.get(key)
            if isinstance(value, list):
                return [r for r in value if isinstance(r, dict)]
    return []


@router.get("/bom-pending")
def tplus_bom_pending(
    scope: Literal["mine", "all"] = Query(default="mine"),
    user: dict[str, Any] = Depends(require_login),
) -> dict[str, Any]:
    _require_bom_audit(user)
    items: list[dict[str, Any]] = []
    if scope == "mine":
        seen: set[tuple[str, str]] = set()
        for request_json in _load_success_submissions():
            key = submission_bom_key(request_json or {})
            if not key or key in seen:
                continue
            seen.add(key)
            code, version = key
            rows = _bom_query_rows(_chanjet_read_post(BOM_QUERY_ENDPOINT, {"dto": {"Code": code, "Version": version}}))
            for row in rows:
                if voucher_is_pending(row):
                    items.append(pending_item(row))
    else:
        page = 1
        while True:
            rows = _bom_query_rows(_chanjet_read_post(
                BOM_QUERY_ENDPOINT,
                {"param": {"PageSize": 200, "PageIndex": page,
                           "SelectFields": "ID,Code,Version,Inventory.Name,ProduceQuantity,VoucherState.Code,VoucherState.Name,BOMChildDTOs"}},
            ))
            items.extend(pending_item(r) for r in rows if voucher_is_pending(r))
            if len(rows) < 200:
                break
            page += 1
            if page > 50:
                break
    return {"items": items, "scope": scope, "synced_at": datetime.now(timezone.utc).isoformat()}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_backend_tplus_bom_picker.py -q`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add services/backend-api/app/routers/tplus_bom.py tests/test_backend_tplus_bom_picker.py
git commit -m "feat(tplus): bom-pending 端点（mine/all 实时查 T+ 未审）"
```

---

### Task 4: `POST /v1/tplus/bom-audit` 端点（真审核 + 复查）

**Files:**
- Modify: `services/backend-api/app/routers/tplus_bom.py`
- Test: `tests/test_backend_tplus_bom_picker.py`

**Interfaces:**
- Consumes: Task 2 `build_audit_payload`/`voucher_is_pending`；Task 3 `_bom_query_rows`/`BOM_QUERY_ENDPOINT`/`BOM_AUDIT_ENDPOINT`。
- Produces: `POST /v1/tplus/bom-audit` body `{code, version, bom_id?}` → `{"audited": bool, "voucher_state": {...}}`；新增 `_chanjet_business_post()`（T+ POST，返回 (body, business_message)）。

- [ ] **Step 1: 写失败测试**

`tests/test_backend_tplus_bom_picker.py` 的 `TPlusBomPendingTests` 类内追加：

```python
    def test_audit_success_when_recheck_flips_to_audited(self):
        calls = []
        def fake_audit_post(endpoint, payload):
            calls.append(endpoint)
            return {"result": "ok"}, ""
        def fake_query(endpoint, payload):
            return [{"Code": "06000001", "Version": "V1", "ID": 1, "VoucherState": {"Code": "01", "Name": "已审"}}]
        self.main._chanjet_business_post = fake_audit_post
        self.main._chanjet_read_post = fake_query
        body = self.main.BomAuditBody(code="06000001", version="V1", bom_id="1")
        result = self.main.tplus_bom_audit(body=body, user=self._user())
        self.assertTrue(result["audited"])
        self.assertEqual("01", result["voucher_state"]["code"])
        self.assertIn(self.main.BOM_AUDIT_ENDPOINT, calls)

    def test_audit_not_audited_when_recheck_still_pending(self):
        self.main._chanjet_business_post = lambda endpoint, payload: ({}, "存货记账中，暂不能审核")
        self.main._chanjet_read_post = lambda endpoint, payload: [
            {"Code": "06000001", "Version": "V1", "ID": 1, "VoucherState": {"Code": "00", "Name": "未审"}}]
        body = self.main.BomAuditBody(code="06000001", version="V1", bom_id="1")
        result = self.main.tplus_bom_audit(body=body, user=self._user())
        self.assertFalse(result["audited"])
        self.assertIn("记账中", result["message"])

    def test_audit_requires_permission(self):
        from fastapi import HTTPException
        body = self.main.BomAuditBody(code="06000001", version="V1", bom_id="1")
        with self.assertRaises(HTTPException) as ctx:
            self.main.tplus_bom_audit(body=body, user={"sub": "x", "roles": [], "permissions": []})
        self.assertEqual(403, ctx.exception.status_code)
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_backend_tplus_bom_picker.py::TPlusBomPendingTests -q`
Expected: FAIL（无 `BomAuditBody`/`tplus_bom_audit`/`_chanjet_business_post`）

- [ ] **Step 3: 实现**

`BomAuditBody` 模型（放在其它 Body 模型附近）：

```python
class BomAuditBody(BaseModel):
    code: str = Field(min_length=1, max_length=100)
    version: str = Field(min_length=1, max_length=100)
    bom_id: str = Field(default="", max_length=40)
```

`_chanjet_business_post`（模块级，仿 `_chanjet_read_post` 但捕获 HTTPError 体、抽业务原文）：

```python
_AUDIT_MESSAGE_KEYS = ("message", "Message", "msg", "Msg", "error", "Error", "detail", "Detail")


def _extract_business_message(text: str) -> str:
    try:
        data = json.loads(text)
    except (ValueError, TypeError):
        return ""

    def walk(node: Any) -> str:
        if isinstance(node, dict):
            for key in _AUDIT_MESSAGE_KEYS:
                value = node.get(key)
                if isinstance(value, str) and value.strip():
                    return value.strip()
            for value in node.values():
                nested = walk(value)
                if nested:
                    return nested
        elif isinstance(node, list):
            for value in node:
                nested = walk(value)
                if nested:
                    return nested
        return ""

    return walk(data)


def _chanjet_business_post(endpoint: str, payload: dict[str, Any]) -> tuple[Any, str]:
    app_key = os.getenv("CHANJET_APP_KEY", "").strip()
    app_secret = os.getenv("CHANJET_APP_SECRET", "").strip()
    open_token = _chanjet_open_token()
    if not app_key or not app_secret or not open_token:
        raise HTTPException(status_code=503, detail="T+ 查询凭据未配置")
    base_url = os.getenv("CHANJET_BASE_URL", "https://openapi.chanjet.com").rstrip("/")
    request = urllib.request.Request(
        f"{base_url}/{endpoint.lstrip('/')}",
        data=json.dumps(payload, ensure_ascii=False).encode("utf-8"),
        headers={"Content-Type": "application/json", "appKey": app_key, "appSecret": app_secret, "openToken": open_token},
        method="POST",
    )
    try:
        with urllib.request.urlopen(request, timeout=15) as response:
            return json.loads(response.read().decode("utf-8")), ""
    except urllib.error.HTTPError as exc:
        body = exc.read().decode("utf-8", errors="replace")
        return None, _extract_business_message(body) or f"T+ 返回 HTTP {exc.code}"
    except (urllib.error.URLError, TimeoutError, ValueError) as exc:
        raise HTTPException(status_code=502, detail=f"T+ 审核请求失败：{type(exc).__name__}") from exc


@router.post("/bom-audit")
def tplus_bom_audit(body: BomAuditBody, user: dict[str, Any] = Depends(require_login)) -> dict[str, Any]:
    _require_bom_audit(user)
    _body, message = _chanjet_business_post(BOM_AUDIT_ENDPOINT, build_audit_payload(body.code, body.version, body.bom_id))
    rows = _bom_query_rows(_chanjet_read_post(BOM_QUERY_ENDPOINT, {"dto": {"Code": body.code, "Version": body.version}}))
    state = (rows[0].get("VoucherState") if rows else {}) or {}
    audited = bool(rows) and not voucher_is_pending(rows[0])
    if audited:
        _audit(_actor(user), "tplus.bom.audit", "tplus_bom", f"{body.code}@{body.version}")
    return {
        "audited": audited,
        "voucher_state": {"code": _text_state(state, "Code"), "name": _text_state(state, "Name")},
        "message": "" if audited else (message or "审核后复查仍为未审，请刷新重试"),
    }


def _text_state(state: dict[str, Any], key: str) -> str:
    return str(state.get(key) or "").strip()
```

（注：`_text_state` 是本文件内小工具，避免从 domain 模块引私有 `_text`。）

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_backend_tplus_bom_picker.py -q`
Expected: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add services/backend-api/app/routers/tplus_bom.py tests/test_backend_tplus_bom_picker.py
git commit -m "feat(tplus): bom-audit 端点（真调 T+ 审核+复查 VoucherState）"
```

---

### Task 5: 前端——录入/审核 Tab + 审核列表

**Files:**
- Modify: `services/public-web/bom-builder/index.html`
- Test: `tests/test_bom_builder_page.py`

**Interfaces:**
- Consumes: Task 3 `GET /v1/tplus/bom-pending?scope=`；Task 4 `POST /v1/tplus/bom-audit`。
- Produces: DOM 锚点 `entryTab`/`auditTab`/`tabEntry`/`tabAudit`/`auditList`/`auditScope`/`refreshAuditBtn`；JS `switchTab`/`loadPending`/`auditRow`。

- [ ] **Step 1: 写失败测试**

`tests/test_bom_builder_page.py` 追加：

```python
def test_entry_audit_tabs_present():
    html = read_page()
    for anchor in ("tabEntry", "tabAudit", "entryTab", "auditTab"):
        assert f'id="{anchor}"' in html, anchor
    assert "function switchTab(" in html
    assert "录入" in html and "审核" in html


def test_audit_list_wiring():
    html = read_page()
    assert "/v1/tplus/bom-pending" in html
    assert "/v1/tplus/bom-audit" in html
    assert 'id="auditList"' in html
    assert 'id="auditScope"' in html
    assert 'id="refreshAuditBtn"' in html
    assert "function loadPending(" in html
    assert "function auditRow(" in html
    assert "本工具建的" in html and "T+ 全部未审" in html
    assert "当前没有待审核" in html


def test_audit_row_confirm_and_recheck_semantics():
    html = read_page()
    # 审核前二次确认 + 审核后按 audited 结果处理（防假装：不假设成功）
    assert "确认审核" in html
    assert "data.audited" in html
    assert "switchTab('audit')" in html  # 提交成功自动跳审核
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_bom_builder_page.py -q`
Expected: 新测试 FAIL

- [ ] **Step 3: 实现 HTML 结构**

CSS 区（`details.group` 附近）加：

```css
    .tabs{display:flex;gap:8px;margin-bottom:14px}
    .tab{flex:1;text-align:center;padding:10px;border-radius:12px;background:#e6ece6;font-weight:650;cursor:pointer;border:1px solid var(--line)}
    .tab.active{background:var(--primary);color:#fff}
    .audit-row{border:1px solid var(--line);border-radius:12px;padding:10px 12px;margin-bottom:8px;background:#fff}
    .audit-row.gone{opacity:0;transition:opacity .5s}
    .audit-head{display:flex;align-items:center;gap:8px;flex-wrap:wrap}
    .audit-children{margin-top:8px;border-top:1px dashed var(--line);padding-top:8px}
    .audit-child{display:flex;justify-content:space-between;font-size:13px;color:var(--muted);padding:2px 0}
```

`<main class="wrap">` 内、`<div id="msg">` 之后、`<div class="layout">` 之前插入 Tab 条：

```html
    <div class="tabs">
      <div id="tabEntry" class="tab active" role="button">录入</div>
      <div id="tabAudit" class="tab" role="button">审核</div>
    </div>
```

把现有 `<div class="layout">…</div>`（父件+子件两 panel）整体包进 `<div id="entryTab">…</div>`（含吸底提交条相关的录入内容保持不动）。

在 `entryTab` 之后加审核 Tab：

```html
    <div id="auditTab" class="hidden">
      <section class="panel">
        <div class="row" style="justify-content:space-between;align-items:center">
          <div class="row" style="gap:6px">
            <label class="small"><input type="radio" name="auditScope" value="mine" checked/> 本工具建的</label>
            <label class="small"><input type="radio" name="auditScope" value="all"/> T+ 全部未审</label>
          </div>
          <button id="refreshAuditBtn" class="btn" type="button">刷新</button>
        </div>
        <div id="auditSyncedAt" class="small muted" style="margin:6px 0"></div>
        <div id="auditList"></div>
      </section>
    </div>
```

- [ ] **Step 4: 实现 JS**

script 区加（`switchTab` 放装配前）：

```js
    function switchTab(name){
      const audit=name==='audit';
      $('entryTab').classList.toggle('hidden',audit);
      $('auditTab').classList.toggle('hidden',!audit);
      $('tabEntry').classList.toggle('active',!audit);
      $('tabAudit').classList.toggle('active',audit);
      document.querySelector('.sticky-bar').classList.toggle('hidden',audit);
      if(audit)loadPending();
    }
    function auditScope(){return (document.querySelector('input[name=auditScope]:checked')||{}).value||'mine';}
    async function loadPending(){
      if(!token()){$('auditList').innerHTML='<div class="muted small">请先登录（SSO）。</div>';return}
      $('auditList').innerHTML='<div class="muted small">加载中…</div>';
      try{const data=await api(`/v1/tplus/bom-pending?scope=${auditScope()}`);
        $('auditSyncedAt').textContent=`同步于 ${new Date(data.synced_at).toLocaleTimeString('zh-CN')}`;
        renderPending(data.items||[]);}
      catch(e){$('auditList').innerHTML=`<div style="color:var(--danger)" class="small">加载失败：${esc(e.message)}（点刷新重试）</div>`;}}
    function renderPending(items){
      if(!items.length){$('auditList').innerHTML='<div class="muted small">当前没有待审核 BOM。</div>';return}
      $('auditList').innerHTML=items.map((it,i)=>`<div class="audit-row" data-i="${i}">
        <div class="audit-head"><b>${esc(it.code)}</b> <span>${esc(it.name)}</span>
          <span class="tag">版本 ${esc(it.version)}</span><span class="tag">生产 ${esc(it.produce_quantity||'-')}</span>
          <span class="status" style="margin-left:auto">${esc(it.voucher_state.name||'未审')}</span></div>
        <button class="linklike" data-toggle="${i}" type="button">展开子件（${(it.children||[]).length}）</button>
        <div class="audit-children hidden" data-children="${i}">${(it.children||[]).map((c)=>`<div class="audit-child"><span>${esc(c.code)} ${esc(c.name)}</span><span>${esc(c.required_quantity)} ${esc(c.unit_name)}</span></div>`).join('')||'<div class="muted small">无子件</div>'}</div>
        <div style="margin-top:8px"><button class="primary" data-audit="${i}" type="button">审核</button></div>
        <div class="small" data-err="${i}" style="color:var(--danger);margin-top:6px"></div></div>`).join('');
      $('auditList').querySelectorAll('[data-toggle]').forEach((el)=>el.onclick=()=>$('auditList').querySelector(`[data-children="${el.dataset.toggle}"]`).classList.toggle('hidden'));
      $('auditList').querySelectorAll('[data-audit]').forEach((el)=>el.onclick=()=>auditRow(items[Number(el.dataset.audit)],el.closest('.audit-row')));
    }
    async function auditRow(item,rowEl){
      if(!confirm(`确认审核：${item.code}（${item.name}）版本 ${item.version}，${(item.children||[]).length} 个子件？`))return;
      const errEl=rowEl.querySelector('[data-err]');errEl.textContent='';
      const btn=rowEl.querySelector('[data-audit]');btn.disabled=true;
      try{const data=await api('/v1/tplus/bom-audit',{method:'POST',body:JSON.stringify({code:item.code,version:item.version,bom_id:item.bom_id})});
        if(data.audited){rowEl.classList.add('gone');setTimeout(()=>loadPending(),600);}
        else{errEl.textContent=`未通过：${esc(data.message||'复查仍未审')}`;btn.disabled=false;}}
      catch(e){errEl.textContent=`审核失败：${esc(e.message)}`;btn.disabled=false;}}
```

装配区（DOMContentLoaded 内）加：

```js
      $('tabEntry').onclick=()=>switchTab('entry');
      $('tabAudit').onclick=()=>switchTab('audit');
      $('refreshAuditBtn').onclick=loadPending;
      document.querySelectorAll('input[name=auditScope]').forEach((el)=>el.addEventListener('change',loadPending));
```

`pollSubmission` 的 success 分支（`message('T+ BOM 创建成功…','good')` 处）追加 `switchTab('audit');`。

- [ ] **Step 5: 跑测试确认通过**

Run: `python -m pytest tests/test_bom_builder_page.py tests/test_tplus_bom_builder_frontend.py -q`
Expected: 全部 PASS

- [ ] **Step 6: JS 语法自检 + Playwright 双 Tab 冒烟**

抽内联 JS 过 `node --check`；Playwright（channel=chrome, file://）验证：默认录入 Tab 可见、点「审核」切换、审核 Tab 空态无 JS 错误。

Run（节点检查）：
```bash
python - <<'PY'
import pathlib
html=pathlib.Path('services/public-web/bom-builder/index.html').read_text(encoding='utf-8')
js=html.split('<script>')[1].split('</script>')[0]
pathlib.Path('scratch_bom.js').write_text(js,encoding='utf-8')
PY
node --check scratch_bom.js && echo JS-OK && rm scratch_bom.js
```
Expected: JS-OK

- [ ] **Step 7: 提交**

```bash
git add services/public-web/bom-builder/index.html tests/test_bom_builder_page.py
git commit -m "feat(bom-builder): 录入/审核 Tab + 审核列表（实时未审/行内子件/真审核复查）"
```

---

### Task 6: 全量回归 + VERSION/CHANGELOG + PR

**Files:**
- Modify: `VERSION`（→ v2.1.17）、`CHANGELOG.md`

- [ ] **Step 1: backend 全量**

Run: 仓库根 `python -m pytest tests/ -q`
Expected: 全部 PASS（formula 前端 4 例已在 #187 修好，不应再红）

- [ ] **Step 2: VERSION + CHANGELOG**

`VERSION` → `v2.1.17`。`CHANGELOG.md` 顶部（`# 版本记录` 之后）插入：

```markdown
## v2.1.17：bom-builder 增加 BOM 审核（录入/审核 Tab）

- 同页顶部加 `录入 / 审核` Tab；提交成功自动跳审核页。
- `GET /v1/tplus/bom-pending?scope=mine|all`：实时查 T+ 未审 BOM（mine=本工具 success 提交逐条 bom/Query 过滤未审；all=列表分页过滤），返回含子件明细。
- `POST /v1/tplus/bom-audit`：真调 T+ bom/Audit 后立即 bom/Query 复查 VoucherState，翻已审才 audited=true 并移除，否则透传 T+ 原文。
- 新权限 `tplus.bom.audit`（迁移 0028，授 admin）。审核列表行内可展开看子件、逐条确认审核、审核后同步消失、手动刷新。
```

- [ ] **Step 3: 验证版本 + 提交**

Run: `python scripts/validate_version.py`
Expected: 通过

```bash
git add VERSION CHANGELOG.md
git commit -m "chore: bump VERSION v2.1.17 + CHANGELOG"
```

- [ ] **Step 4: 推分支 + 开 PR**

```bash
git push -u origin feat/bom-audit
gh pr create --title "feat(bom-builder): BOM 审核（录入/审核 Tab，实时未审+真审核复查）" --body "见 docs/superpowers/specs/2026-07-14-bom-audit-design.md

⚠️ T+ bom/Audit 精确参数形态部署后现场探定（build_audit_payload 单函数），见计划 Task 7。

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

- [ ] **Step 5: 跟 CI + 合并**

Run: `gh pr checks <PR#> --watch`
CI 绿后合并（用户授权则 `gh pr merge <PR#> --squash --delete-branch`）。

---

### Task 7: 部署后现场校准 T+ 审核参数 + 真机端到端（⚠️ 部署完成后做）

**Files:** 可能 Modify: `services/backend-api/app/tplus_bom.py`（仅 `build_audit_payload` 一函数，若探测格式不符）

**前置：** PR 合并 + release-deploy success + T+ 未处于限流。

- [ ] **Step 1: 探定 bom/Audit 成功参数**

在 ecs-backend-api-1 容器内，对一个真实未审测试 BOM（先 `bom/Query` 拿 Code/Version/ID）逐一试审核参数形态：`{"dto":{"ID":id}}`、`{"dto":{"Code":c,"Version":v}}`、`{"dto":{"ID":id,"Code":c,"Version":v}}`，看哪种返回成功（再 `bom/Query` 复查 VoucherState 翻 01）。记录成功形态。

- [ ] **Step 2: 若与 build_audit_payload 不符则改**

仅调整 `build_audit_payload()` 一个函数返回体使之匹配成功形态；跑 `python -m pytest tests/test_tplus_bom_write.py -q`（更新对应断言）→ 提交 → 走 PR 上线。若默认 `{"dto":{"Code","Version","ID"}}` 已成功则无需改动。

- [ ] **Step 3: 真机端到端**

用户在页面：录入并提交一单 → 自动跳审核 Tab 看到该 BOM → 展开看子件明细 → 点审核确认 → 该行消失 → 切「T+ 全部未审」确认它不在了 → 去 T+ 复核该 BOM 已是「已审」。任何一步不符回本计划对应 Task 修。
